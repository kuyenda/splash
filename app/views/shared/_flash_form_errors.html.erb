<% if object.errors.any? %>
  <div class="<%= options[:class] %>">
    <!-- Oops, <%= pluralize(object.errors.count, "error")%>. -->
    <ul>
      <%  object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>