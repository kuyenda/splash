<div class="row">
  <div class="d-none d-md-block col-md-1">
  </div>
  <div class="col-10 offset-md-1">
    <ul>
      <li>
        <p>Selector start with dot</p>
        <p><code>.class_name</code> signifies class name</p>
      </li>
      <li>
        <p>Two doted selector separated by space</p>
        <p><code>.outside</code> <code>.inside</code></p>
        <p>means element with <code>.inside</code> class descended from an element with class <code>.outside</code> </p>
      </li>
      <li>
        <p>Two doted selector without separation</p>
        <p><code>.name1.name2</code> </p>
        <p>means element that has both class <code>name1</code> and <code>name2</code>
          eg: <code>class="name1 name2"</code></p>
      </li>
    </ul>
  </div>
</div>